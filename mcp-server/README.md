# MCP server — unreleased source

This folder is retained for possible future development. It is not part of the `v0.1.0-beta.1` shadcn registry release.

The server is currently unsupported, not advertised by the catalog, and excluded from root lint, typecheck, build, CI, and release claims. Its metadata and install guidance can be stale until MCP work resumes.
